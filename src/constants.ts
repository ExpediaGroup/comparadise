import { StringParam } from 'use-query-params';

export const URL_PARAMS = {
  hash: StringParam,
  bucket: StringParam,
  repo: StringParam,
  owner: StringParam,
  baseImagesDirectory: StringParam
};
